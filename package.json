{
  "name": "fiducia-admin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser E2E for the fiducia-admin dashboard. Playwright + Puppeteer are consumed as libraries under node --test; the Rust server is booted by the shared harness via cargo (no npm build step).",
  "scripts": {
    "test:browser": "node --test --test-concurrency=1 tests/*.test.mjs",
    "test:browser:playwright": "node --test tests/admin-playwright.test.mjs",
    "test:browser:puppeteer": "node --test tests/admin-puppeteer.test.mjs",
    "lint:ores": "if [ -f .ores-lint/lint.sh ]; then sh .ores-lint/lint.sh; else echo 'ores-lint not installed'; fi",
    "prepublishOnly": "npm run lint:ores"
  },
  "devDependencies": {
    "@fiducia/test-config": "file:../fiducia-test-config",
    "playwright": "^1.61.1",
    "puppeteer": "^25.2.1"
  },
  "oresLint": {
    "note": "Declared, not installed. ores-lint resolves these from a global install - see .ores-lint/README.md",
    "requires": {
      "eslint": ">=9",
      "typescript-eslint": ">=8 (optional; without it .ts files are skipped)"
    },
    "install": "npm i -g eslint typescript-eslint"
  }
}
