This reopened temporary pull request includes the enabled ordinary pull-request helper from current `main`. This synchronize event applies the hardcoded patch to admin PR #23 and generates the exact Cargo lockfile.

Synchronize marker: 2026-08-03 dual-auth E2E augmentation.
